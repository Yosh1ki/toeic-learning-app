import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  header: {
    marginBottom: 15,
    alignItems: "center",
  },
  scrollContainer: {
    flex: 1,
  },
});

export default styles;