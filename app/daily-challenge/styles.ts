import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  header: {
    marginBottom: 15,
    alignItems: "center",
  },
  scrollContainer: {
    flex: 1,
  },
})